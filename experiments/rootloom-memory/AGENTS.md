# Rootloom Memory plugin guidance

- This plugin is experimental, separately installed, explicitly invoked, and never a Rootloom Core prerequisite.
- Keep runtime helpers local, bounded, network-free, standard-library-only, and subordinate to current repository evidence.
- Preserve `rootloom-project-memory-v1` compatibility; writes remain explicit, locked, atomic, and reviewable.
