interface:
  display_name: "Rootloom Change"
  short_description: "Complete changes with proportional verification"
  default_prompt: "Use $operating-coding-change to complete this change within its authorized scope, verify the changed behavior, and use bounded artifact reads or reusable receipts as appropriate."
policy:
  allow_implicit_invocation: true
