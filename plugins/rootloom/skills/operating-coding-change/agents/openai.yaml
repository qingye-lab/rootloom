interface:
  display_name: "Rootloom Change"
  short_description: "Route direct, scoped, governed, or evidence changes"
  default_prompt: "Use $operating-coding-change to route this change, fix it at the owning boundary, and verify it proportionally."
policy:
  allow_implicit_invocation: true
