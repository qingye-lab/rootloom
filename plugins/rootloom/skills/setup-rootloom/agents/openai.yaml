interface:
  display_name: "Set Up Rootloom"
  short_description: "Plan, install, inspect, update, or roll back Rootloom"
  default_prompt: "Use $setup-rootloom to plan, install, inspect, update, or roll back Rootloom safely."
policy:
  allow_implicit_invocation: true
